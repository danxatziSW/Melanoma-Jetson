# Core ML
# Install torch/torchvision matching your CUDA setup first (see pytorch.org).
# pip's default index only carries CPU wheels, and the exact build (e.g. +cu128)
# isn't resolvable from PyPI alone.
torch>=2.2.0
torchvision>=0.17.0
timm==1.0.26

# Data & augmentation
albumentations==2.0.8
opencv-python==4.13.0.92
Pillow==12.1.1
numpy==2.4.3
pandas==3.0.2
scikit-learn==1.8.0

# Excel output
openpyxl==3.1.5

# ONNX & quantization
onnx==1.21.0
onnxruntime==1.25.1

# Config
pyyaml==6.0.3

# Optional: YOLOv8 detector/classifier (falls back to a timm MobileNetV3 if absent)
# ultralytics==8.4.56
# onnxslim>=0.1.71   # ultralytics' ONNX exporter pulls this in automatically if missing

# Optional: FLOPs counter fallback
# thop>=0.1.1
